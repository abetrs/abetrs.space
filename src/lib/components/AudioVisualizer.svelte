<script>
	// Clean stub of AudioVisualizer component: renders nothing and contains no canvas
	// This satisfies imports while removing the visualizer implementation as requested.
	// These are kept as const exports so pages that pass props won't error.
	// They are intentionally unused in this stub component.
	export const audioElement = null;
	export const analyser = null;
	export const audioCtx = null;
</script>

<!-- AudioVisualizer removed -->
